apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tote.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tote.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "tote.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "tote.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "tote.serviceAccountName" . }}
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: tote
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - --enabled={{ .Values.config.enabled }}
            - --metrics-addr={{ .Values.config.metricsAddr }}
          ports:
            - name: metrics
              containerPort: {{ (split ":" .Values.config.metricsAddr)._1 | default 8080 }}
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
